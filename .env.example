# Local File Organizer Environment Configuration

# Path to the directory you want to organize
# Example: INPUT_PATH=C:/Users/YourName/Documents
INPUT_PATH=

# Path where organized files will be stored
# If not specified, will default to 'organized_folder' in the input directory
# Example: OUTPUT_PATH=C:/Users/YourName/Documents/Organized
OUTPUT_PATH=

# File extensions to process, comma-separated
# Example: FILE_EXTENSIONS=md,txt,docx,pdf
# If not specified, defaults to .md files only
FILE_EXTENSIONS=

# Set to 'true' to enable silent mode (outputs to log file instead of console)
# Valid values: true, false, yes, no, 1, 0, y, n
# Default: false
SILENT_MODE=false

# Organization mode to use
# Valid values: content, date, type
# If not specified, will prompt the user to select
ORGANIZATION_MODE=

# Note: To use this file, rename it to .env
# If all required values (INPUT_PATH, OUTPUT_PATH, FILE_EXTENSIONS, and ORGANIZATION_MODE) 
# are provided, the script will run automatically without user interaction 